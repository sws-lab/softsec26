// Exercise 3. Convert recursive binary search implementation from exercise 2 into loop-based form.
// Augment the implementation to accept nulls as an array argument, assuming that null is equivalent to empty array.
// Annotate the method with appropriate pre- and postconditions.

method BinarySearch(a: array?<int>, value: int) returns (index: int)
	// Method contract
{
	// Fill in the body that performs binary search
}
